import { GameStatus, Sport } from '../types.js';

/**
 * DE-MOCKED: INITIAL_GAMES is now empty.
 * The system is forced to fetch real-time data from the backend or external APIs.
 */
export const INITIAL_GAMES = [];

export const generateMockHistory = () => {
    // Returning empty or minimal for stability, but we prefer real history from backend
    return [];
};